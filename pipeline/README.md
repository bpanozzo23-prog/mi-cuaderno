# pipeline

Home of the reference-data pipeline (brief §12, Phase 2). The Phase 0.5 feasibility spike
lives in `spike/`. Raw downloads go to `spike/raw/`, which is gitignored — scripts and small
generated outputs are committed, multi-hundred-MB source files are not.

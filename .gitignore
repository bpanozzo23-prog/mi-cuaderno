# dependencies and build output
node_modules/
dist/
dev-dist/

# raw pipeline downloads (hundreds of MB — never committed)
pipeline/spike/raw/
pipeline/raw/

# local assistant/tooling config (machine-specific paths)
.claude/

# local junk
*.local
.DS_Store
Thumbs.db

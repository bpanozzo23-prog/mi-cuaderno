# dependencies and build output
node_modules/
dist/
dev-dist/

# raw pipeline downloads (hundreds of MB — never committed)
pipeline/spike/raw/
pipeline/raw/

# local assistant/tooling config (machine-specific paths)
.claude/

# notebook backups — personal data, never committed (the repo is public)
mi-cuaderno-backup-*.json
before-import-*.json

# local junk
*.local
.DS_Store
Thumbs.db
